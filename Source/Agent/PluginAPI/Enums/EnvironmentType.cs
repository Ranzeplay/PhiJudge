namespace PhiJudge.Agent.API.Plugin.Enums
{
    /// <summary>
    /// Decide whether the class is for the host environment or the container environment.
    /// </summary>
    public enum EnvironmentType
    {
        Host,
        Container
    }
}
